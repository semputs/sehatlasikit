name: Auto-Sync Dashboard

on:
  schedule:
    - cron: '0 13 * * *' # Daily at 9:00 PM MYT (13:00 UTC)
  workflow_dispatch:

permissions:
  contents: write # Grants commit & push access

jobs:
  sync-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: pip install requests

      - name: Fetch Google Sheet & Update index.html
        env:
          SHEET_CSV_URL: ${{ secrets.SHEET_CSV_URL }}
        run: python agent.py

      - name: Commit & Push Changes
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add index.html
          git diff --quiet || git commit -m "Auto-sync spreadsheet logs to dashboard"
          git push
          
